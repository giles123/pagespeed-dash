version: "3.7"

services:
  app:
    build: docker/app
    working_dir: /usr/src/app
    volumes:
      - .:/usr/src/app
    ports:
      - "80:80"
    depends_on:
    - db
  db:
    image: mongo
    ports:
      - "27017:27017"